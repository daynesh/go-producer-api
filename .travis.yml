language: go

go:
    - 1.8.x

install:
    - make install

script:
    - ./.travis.gofmt.sh
    - make testcoverage # Runs unit tests & code coverage

after_success:
    - bash <(curl -s https://codecov.io/bash)