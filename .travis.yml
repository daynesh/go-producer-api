language: go

go:
    - 1.8.x

install:
    - make install

script:
    - ./.travis.gofmt.sh # Fail if .go file hasn't been formatted with gofmt
    #- ./.travis.govet.sh
    #- ./.travis.golint.sh
    - make testcoverage  # Runs unit tests & code coverage

after_success:
    - bash <(curl -s https://codecov.io/bash)